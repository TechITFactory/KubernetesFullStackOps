apiVersion: v1
kind: Pod
metadata:
  name: pod-hostname-demo
spec:
  hostname: custom-host
  subdomain: demo-subdomain
  containers:
    - name: app
      image: busybox:1.36
      command: ["sh", "-c", "hostname && sleep 3600"]
