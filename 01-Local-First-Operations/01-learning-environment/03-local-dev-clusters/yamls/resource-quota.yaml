apiVersion: v1
kind: ResourceQuota
metadata:
  name: dev-local-quota
  namespace: dev-local
spec:
  hard:
    requests.cpu: "2"
    requests.memory: 2Gi
    limits.cpu: "4"
    limits.memory: 4Gi
    pods: "20"
