apiVersion: v1
kind: Service
metadata:
  name: nginx-demo
  namespace: stateless-lab
  labels:
    app: nginx-demo
spec:
  selector:
    app: nginx-demo
  ports:
    - name: http
      port: 80
      targetPort: 80
      nodePort: 30080
  type: NodePort
