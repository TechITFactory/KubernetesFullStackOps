apiVersion: v1
kind: ConfigMap
metadata:
  name: mixed-version-proxy-notes
  namespace: kube-system
data:
  notes: |
    Mixed-version proxy concepts matter mostly during upgrades and compatibility windows across skewed components.
