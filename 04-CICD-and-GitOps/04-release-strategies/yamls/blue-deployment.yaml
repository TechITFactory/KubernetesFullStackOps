apiVersion: apps/v1
kind: Deployment
metadata:
  name: blue-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: colors
      version: blue
  template:
    metadata:
      labels:
        app: colors
        version: blue
    spec:
      containers:
      - name: web
        image: nginx:1.24
